# =============================================================================
# Production environment overrides.
# Activated via -Denv=prod
# =============================================================================

env=prod

# Tighter timeouts in production — failures should surface fast
api.default.timeout.ms=3000
retry.count=2
