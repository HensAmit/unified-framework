# =============================================================================
# Development environment overrides.
# Activated via -Denv=dev (the default if no env is specified).
# =============================================================================

env=dev

# More forgiving timeouts during development
api.default.timeout.ms=10000
retry.count=1
