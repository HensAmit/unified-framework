# Build artifacts — rebuilt inside the image; never copy host-compiled classes.
**/target/

# Version control and IDE noise.
.git/
.gitignore
.idea/
*.iml
.vscode/

# OS / logs.
.DS_Store
*.log

# Docker files themselves don't need to be in the build context.
Dockerfile
docker-compose*.yml
.dockerignore
